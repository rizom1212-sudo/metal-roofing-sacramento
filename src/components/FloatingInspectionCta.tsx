/**
 * Desktop floating estimate chip removed: nav CTA + phone, in-page forms,
 * and the mobile sticky bar already cover conversion. Kept as a no-op so
 * Layout does not need a routing change.
 */
export default function FloatingInspectionCta() {
  return null;
}
